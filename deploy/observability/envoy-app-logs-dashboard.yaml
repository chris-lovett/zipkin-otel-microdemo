apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: envoy-app-logs
  namespace: observability
spec:
  instanceSelector:
    matchLabels:
      dashboards: grafana
  configMapRef:
    name: envoy-app-logs-dashboard
    key: envoy-app-logs-dashboard.json

# Made with Bob
